# Optional: override the default public RPC with Alchemy/Infura for better reliability.
# Default is https://eth.llamarpc.com (no key required).
ETH_RPC_URL=

# Optional: starting block for historical AsteroidBurned event scan.
# If unset, the server will scan the last ~200k blocks (roughly 28 days).
EVENT_START_BLOCK=
