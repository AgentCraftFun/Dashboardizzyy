# public/

Drop static assets here — they're served from the site root.

## Logo

Save your logo file as **`public/logo.png`** (a square PNG, 512×512 or similar
works well). The header and hero reference `/logo.png` and will automatically
fall back to the 🪐 emoji badge if the file is missing, so missing-logo will
never break a deploy.
